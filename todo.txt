- Save memory either before quit or after every chat interaction.
- Load above memory on start.
- After every Agent loop, sumamrize the previous messages. Create one message from it. 
- Add "continuity" step after every agent loop. Where AI can reflect and device next decision. (should stop/show results/should prompt itself/ should call a tool?)
- fix issue where max iterations hit still cant update NONE tools
- Fix debug messages
- Check why always call the same tools
- Add summary after every 5 user message
- Fix case where max iterations is hit ad should continue true
- Create file error, but actually does success